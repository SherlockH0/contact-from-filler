services:
  cold-outreach:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - HEADLESS=${HEADLESS:-true}
    networks:
      - n8n

networks:
  n8n:
    external: true
